// App.tsx is now a minimal shell — all logic has moved to:
// - src/routes.tsx (route definitions)
// - src/components/layout/AppShell.tsx (authenticated app)
// - src/components/layout/PublicLayout.tsx (public pages)
// This file is kept for backward compatibility but is no longer used as the entry point.

export default function App() {
  return null;
}
